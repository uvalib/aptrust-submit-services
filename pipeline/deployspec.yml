#
# deployspec.yml
#

version: 0.2

env:
  variables:
    terraform_version: 1.14.6

phases:
  install:
    runtime-versions:
      golang: 1.x
    commands:
      - wget https://releases.hashicorp.com/terraform/${terraform_version}/terraform_${terraform_version}_linux_amd64.zip
      - unzip terraform_${terraform_version}_linux_amd64.zip
      - mv terraform /usr/local/bin

  pre_build:
    commands:
      - git clone https://$GITLAB_USER:$GITLAB_TOKEN@gitlab.com/uvalib/terraform-infrastructure.git

  build:
    commands:
      #
      # apt-submit-bagger
      #- cd $CODEBUILD_SRC_DIR/terraform-infrastructure/aptrust-submit/ecs-tasks/staging/apt-submit-bagger
      #- terraform init -no-color
      #- terraform apply -no-color -auto-approve
      #
      # apt-submit-reconciler
      #- cd $CODEBUILD_SRC_DIR/terraform-infrastructure/aptrust-submit/ecs-tasks/staging/apt-submit-reconciler
      #- terraform init -no-color
      #- terraform apply -no-color -auto-approve
      #
      # apt-submit-submitter
      #- cd $CODEBUILD_SRC_DIR/terraform-infrastructure/aptrust-submit/ecs-tasks/staging/apt-submit-submitter
      #- terraform init -no-color
      #- terraform apply -no-color -auto-approve
      #
      # apt-submit-validator
      - cd $CODEBUILD_SRC_DIR/terraform-infrastructure/aptrust-submit/ecs-tasks/staging/apt-submit-validator
      - terraform init -no-color
      - terraform apply -no-color -auto-approve

#  post_build:
#    commands:

#
# end of file
#
